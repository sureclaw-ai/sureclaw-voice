# Vendored deps and build scratch. Note: the plugin's compiled entry (dist/) and
# the built web app (webapp/) ARE committed — they're the install artifacts.
node_modules/
app/node_modules/
app/dist/
app/.wrangler/
*.tsbuildinfo
*.bak
.DS_Store

# npm lockfiles are not used; pnpm-lock.yaml is committed at the repo root.
package-lock.json
app/package-lock.json
yarn.lock
